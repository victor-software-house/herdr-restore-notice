---
"herdr-restore-notice": patch
---

Hide resume ticket hashes behind `display_agent`, print the same notice when a live agent exits with a retained native session, and add a workspace action that lists those retained sessions.
